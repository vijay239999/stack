import type { DockerComposeConfigs, DockerfileConfigs } from '../../@types/container.js';
import { DockerCompose, DockerContainer } from '../../services/container.js';
export declare const docker: {
    dockerfile: (configs: DockerfileConfigs) => DockerContainer;
    compose: (configs: DockerComposeConfigs) => DockerCompose;
};
